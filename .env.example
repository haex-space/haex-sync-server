# Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Test Database (optional - if not set, tests will use DATABASE_URL with isolated test data)
# Recommended: Use a separate database for tests to avoid any risk of production data pollution
# DATABASE_URL_TEST=postgresql://postgres:postgres@localhost:5432/postgres_test

# Supabase Configuration (Required)
SUPABASE_URL=http://localhost:8000
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU

# Server Configuration
PORT=3000
NODE_ENV=development

# Storage Credentials Encryption Key (Required for S3 proxy)
# Used to encrypt user S3 secret keys at rest with pgcrypto
# Generate with: openssl rand -base64 32
STORAGE_ENCRYPTION_KEY=your-32-byte-base64-encoded-key-here

# MinIO Configuration (S3 storage backend)
MINIO_ENDPOINT=http://localhost:9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# CORS Configuration
# Comma-separated list of allowed origins, or * for all
CORS_ORIGIN=http://localhost:3003,http://localhost:5173

# Docker Port Configuration (for docker-compose.yml)
# Supabase Kong API Gateway
KONG_HTTP_PORT=8000

# Supabase Studio
STUDIO_PORT=3001

# PostgreSQL
POSTGRES_PORT=5432
